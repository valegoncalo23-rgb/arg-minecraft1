# Código de Acesso Secreto
# Complete o código para revelar a frase

def acesso_secreto(chave):
    # O jogador precisa descobrir o que colocar aqui
    if chave == "":  # <-- parte que falta
        return "&t=1s"
    else:
        return "Chave incorreta!"

# Teste
entrada = input("Digite a chave de acesso: ")
print(acesso_secreto(entrada))
